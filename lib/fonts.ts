import { Dancing_Script } from "next/font/google";

/** Handwriting face used for the "Begüm" signature on the ID card. */
export const signatureFont = Dancing_Script({
  subsets: ["latin", "latin-ext"],
  weight: ["700"],
  display: "swap",
});
